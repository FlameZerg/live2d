# live2d (自用)

本仓库存放一组可在浏览器中按需加载的 Live2D Cubism 2 模型资源。每个子目录对应一个独立模型，入口为该目录下的 `*.model.json` 或 `model.json`。

## 来源说明

本仓库中的模型资源源于 [zenghongtu/live2d-model-assets](https://github.com/zenghongtu/live2d-model-assets)（PPet 的 Model 源）。